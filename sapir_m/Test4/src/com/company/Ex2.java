package com.company;

/**
 * Created by hackeru on 16.02.2017.
 */
public class Ex2 {

    //a

    /*public static void bubbleSortLinkedList(Node n) {
        int size = 1;
        Node temp = new Node(0);
        Node temp2 = new Node(0);

        while (n.next != null){
            size++;
            n=n.next;
        }

        int upTo = size; // האיבר האחרון
        boolean isSorted = false;

        while (!isSorted) {
            isSorted = true;
            int i = 1;
            while (i < size) {

                if (n.value > n.next.value) {
                    temp.next = n.next; // prev
                    n.next = n.next.next;
                    temp2 = n.next;
                    temp2.next = n;
                    isSorted = false;
                }
                temp = temp2;
                n = n.next;
                i++;
            }
        }
        upTo--;
    }



    static void insertionSort(int arr[]){
        for (int i = 1; i < arr.length; i++) {
            int key = arr[i];
            int j = i-1;
            while(j>=0 && arr[j] > key){
                arr[j+1] = arr[j];
                j--;
            }
            arr[j+1] = key;
        }
    }

    public static void insertionSortLinkedList(Node n){*/




    }








